# Example

To run this example:

## Installation

- `npm install` or `yarn`

## Dev mode

- `npm dev` or `yarn`

## Build

- `npm build` or `yarn`

## Start build or production

- `npm start` or `yarn start`
